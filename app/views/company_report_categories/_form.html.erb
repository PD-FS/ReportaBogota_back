<%= form_with(model: company_report_category, local: true) do |form| %>
  <% if company_report_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_report_category.errors.count, "error") %> prohibited this company_report_category from being saved:</h2>

      <ul>
      <% company_report_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.text_field :company_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
