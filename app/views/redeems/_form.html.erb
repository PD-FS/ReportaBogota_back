<%= form_with(model: redeem, local: true) do |form| %>
  <% if redeem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(redeem.errors.count, "error") %> prohibited this redeem from being saved:</h2>

      <ul>
      <% redeem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
